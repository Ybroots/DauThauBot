# Railway: ưu tiên build bằng Dockerfile (Playwright). Nếu dùng Nixpacks, Procfile đã set PYTHONPATH=src.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python run_railway.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
