# Chỉ dùng trong Docker — KHÔNG cài playwright qua pip (dùng bản có sẵn trong image).
httpx[http2]>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
pyyaml>=6.0
python-dotenv>=1.0
apscheduler>=3.10
loguru>=0.7
pytz>=2024.1
tzdata>=2024.1
tenacity>=8.2
