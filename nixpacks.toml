# Fallback khi Railway KHONG dung Dockerfile. Uu tien: Settings → Build → Dockerfile.
# Playwright/reCAPTCHA can Dockerfile — Nixpacks thieu Chromium.

[phases.setup]
nixPkgs = ["python310", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt",
  "pip install .",
]

[start]
cmd = "python run_railway.py"
